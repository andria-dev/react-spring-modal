{
  "name": "workspaces",
  "private": true,
  "workspaces": [
    "react-spring-modal",
    "examples/*"
  ],
  "scripts": {
    "build": "yarn workspace react-spring-modal build",
    "example:ts": "yarn workspace snowpack-typescript start"
  },
  "devDependencies": {
    "destiny": "^0.7.0",
    "prettier": "^2.0.5",
    "shx": "^0.3.2"
  },
  "dependencies": {
    "typescript": "^3.9.5"
  }
}
